<div class="content-container" ng-app="myApp" ng-controller="MainCtrl" ng-init="lower = true">

<div class="waiting"><h2> They are waiting on you! </h2></div>

<div class="table-responsive col-xs-12 col-sm-6 col-md-8">
<table class="table">


  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><div class="game-title" ng-click="reloadRoute('/games/<%=game.id %>')"><%=  game.title %> </div><br>
          <div class="game-description"><%= game.description %></div>
        </td>
          <td>by: </td>

        <td class="game-edit-delete"><%= link_to 'Edit', edit_game_path(game) %> | <%= link_to 'Destroy', game, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!-- <div class="start-game"><a href="#" ng-click="reloadRoute('games/new')">Start a game</a></div> -->
<div class="return-home"><a href="#" ng-click="reloadRoute('/')">Return Home</a></div>

</div>